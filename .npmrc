# Use o repositório oficial do npm
registry=https://registry.npmjs.org/

# Salve sempre as dependências no package.json
save=true

# Defina o escopo do seu pacote para usar o GitHub Packages (se necessário)
# Substitua govinda777 pelo seu usuário ou organização
@govinda777:registry=https://npm.pkg.github.com

# Exemplo: Forçar uso de uma versão específica do Node.js (útil para CI/CD)
node-version=20.11.0
engine-strict=true

# Configuração de cache (opcional)
cache=${HOME}/.npm-cache
